{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "dev": "lerna run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
